# 지금 상태

2026-08-27 · 프로젝트 생성만 된 상태. 코드 없음.

## 무엇을 만들고 있나

시험 기기 인쇄물을 스캔해서 읽고, 검증한 뒤,
정보시스템에 붙여넣을 수 있는 형태로 건네주는 도구.

자세한 배경과 원칙은 [CLAUDE.md](../CLAUDE.md).
전체 설계 정리는 아래 링크.

```text
https://claude.ai/code/artifact/43c7496b-7fe8-4e1c-a24b-49125dc91981
```

## 다음 작업

```text
1  Next.js 뼈대 세우기        create-next-app · Tailwind · tsc 통과 확인
2  PDF 업로드 → 판독 한 판     TOC 분석기 출력 한 종류만
                              결과를 콘솔에 JSON으로 뱉는 것까지
3  QC 시료로 정렬 자가검증      w 는 0 근처, STRE(5) 는 5 근처인가
4  그리드 화면                 판독 결과 표시 · 이상한 칸 색칠
5  클립보드 복사               탭 구분 텍스트로
```

2번까지 되면 이 접근이 되는지 안 되는지 판가름난다.
거기서 정확도가 안 나오면 뒤를 만들 이유가 없으므로 **2번을 먼저 끝낸다.**

## 막혀 있는 것

```text
붙여넣기 형태     시스템의 열 순서와 개수를 모른다
                  이게 정해져야 최종 출력 형태가 확정된다
                  4번까지는 이거 없이도 진행 가능

입력 자릿수       항목별로 소수 몇 자리까지 넣는지 확인 중
                  pH · DO 는 1자리로 보이나 미확인

규정              업무 자료를 외부 API로 보내도 되는지
                  실사용 전에 확인 필요.  개발·시험에는 지장 없음
```

## 먼저 재둘 것

지금 안 재두면 나중에 못 잰다.

```text
인쇄물 한 장을 손으로 입력하는 데 걸리는 시간
한 달에 몇 장이나 하는지
최근 오타로 다시 손본 적이 있는지 · 몇 건인지
```

이 숫자가 "만들 가치가 있었는가"를 판단하는 기준선이 된다.

## 자료

실제 스캔본은 저장소에 올리지 않는다. `samples/` 는 `.gitignore` 대상.
테스트용으로 쓸 것은 지점명과 담당자명을 지운 뒤 `fixtures/` 에 둔다.
