# Lab Sheet Reader

시험 기기가 뽑은 인쇄물을 스캔해서 읽고, 스스로 검증한 뒤,
정보시스템에 붙여넣을 수 있는 형태로 건네주는 도구.

## 배경 — 왜 이런 게 필요한가

수질 검사 기기는 결과를 화면에 엑셀로 띄운다. 그런데 그 기기에 붙은
컴퓨터는 보안 때문에 인터넷도 USB도 막혀 있다. 파일을 꺼낼 방법이 없다.

그래서 지금은 이렇게 한다.

```text
기기 화면의 엑셀  →  인쇄  →  종이를 보며 손으로 다시 입력  →  검수
                              └─ 여기서만 오타가 생긴다 ─┘
```

완벽한 디지털 데이터가 종이와 사람 손을 거쳐 다시 디지털이 된다.
정보가 좋아지는 구간은 하나도 없고, 나빠지는 구간만 있다.
게다가 그 오타를 잡으려고 나중에 성적서를 뽑아 다시 대조하는 일이 또 붙는다.

이 도구는 **종이와 사람 손 사이에 카메라를 끼워 넣는다.** 보안 격리는
그대로 두고, 이미 정상적으로 나오는 인쇄물을 대신 읽어준다.

## 지켜야 할 네 가지

```text
정확성   틀린 값이 조용히 지나가면 안 된다.  모르면 모른다고 말한다
절제     헛경보가 많으면 세 번째부터 아무도 안 본다.  적게, 확실하게
무침습   기존 절차와 보안을 건드리지 않는다.  한 단계만 끼워 넣는다
투명성   왜 그렇게 읽었는지 사람이 원본으로 확인할 수 있어야 한다
```

기능을 더하거나 뺄 때 이 네 가지로 판단한다. 특히 둘째. 재현율(놓치지 않기)보다
정밀도(헛짚지 않기)를 택한다. 몇 개 놓치더라도 "얘가 말하면 진짜다"가
되는 편이 훨씬 쓸모 있다.

## 원리 — 어떻게 동작하나

네 단계다.

```text
1  판별   이 스캔이 무슨 문서인지 알아낸다
2  판독   표를 읽어 시료명과 값을 뽑는다
3  검증   읽은 게 맞는지 스스로 확인한다
4  전달   그리드에 보여주고, 클립보드로 넘긴다
```

3번이 이 도구의 핵심이다. **읽는 것보다 읽은 게 맞는지 확인하는 쪽이 어렵다.**

검증의 주력 장치는 품질관리 시료다. 기기를 돌릴 때는 실제 시료 말고도
세척수와 표준품을 같이 넣는데, 이들은 답이 미리 정해져 있다.

```text
세척수(w)      값이 0에 가까워야 정상
표준품(STRE 5)  값이 5 근처여야 정상
```

그러니 표를 읽고 나서 이 자리들을 확인해보면 된다. 표준품 자리에서
0.1이 읽혔다면 표가 한 줄 밀려 읽힌 것이다. 시험성적서는 시스템에
입력이 끝난 뒤에야 나오므로 입력 전에는 대조할 상대가 없는데,
품질관리 시료를 쓰면 **혼자서도 맞는지 확인**할 수 있다.

> **비유** — 시력검사표 맨 윗줄 같은 것이다. 큰 글자를 못 읽으면
> 아랫줄 결과는 볼 것도 없이 다시 해야 한다.
>
> **안 맞는 지점** — 시력검사표는 사람을 재지만, 여기서는 읽는 쪽(도구)이
> 스스로를 잰다. 그리고 기기가 품질관리 시료를 안 넣고 돌린 회차에는
> 이 장치가 통째로 없다. 그때는 사람이 봐야 한다.

### 이렇게 설명하면 된다

> 기기 출력물을 OCR로 읽는데, OCR은 표에서 행이 밀려 읽히는 사고가 납니다.
> 값은 전부 정상 범위라 검증 규칙에도 안 걸리고요. 그래서 실험 배치에
> 원래 들어있는 QC 시료를 앵커로 씁니다. 블랭크는 0 근처, 표준품은 알려진
> 농도 근처여야 하니까, 그 자리 값을 보면 정렬이 맞는지 즉시 판정됩니다.
> 외부 대조 자료 없이 판독 결과를 자기 검증하는 구조입니다.

## 용어

```text
OCR          사진 속 글자를 컴퓨터가 읽어 텍스트로 바꾸는 기술
             Optical Character Recognition. "글자 알아보기"

파서         특정 형식의 문서에서 원하는 값을 뽑아내는 코드 조각
             문서 종류마다 하나씩 둔다

템플릿       "이 문서는 이렇게 생겼다"를 적어둔 설정 파일
             판별 조건 · 열 정의 · 검증 규칙이 한 파일에 들어간다
             화면의 표는 이 파일에서 자동으로 만들어진다

QC 시료      품질관리(Quality Control)용 시료
             세척수 · 바탕시료 · 표준품처럼 답이 정해진 것들
             기기가 제대로 돌아가는지 확인하려고 같이 넣는다
             이 도구에서는 판독이 맞는지 확인하는 데 쓴다

헛경보       틀리지 않았는데 "틀렸다"고 알리는 것
             False Positive. 이게 잦으면 아무도 안 본다

정렬         표에서 어느 시료명과 어느 값이 짝인지
             한 칸 밀리면 모든 값이 엉뚱한 시료에 붙는다.  가장 위험한 실패

전사         한쪽에 적힌 것을 다른 쪽에 옮겨 적는 일
             지금 사람이 하는 그 일. 이 도구가 대신하려는 것
```

## 실무 — 여기서는 이렇게 쓴다

```text
입력   복합기 스캔 PDF.  300dpi · 회색조 · 여러 장을 한 파일로
       접수번호 한 뭉치가 파일 하나가 된다

처리   페이지마다 문서 종류를 판별하고, 종류별 템플릿으로 읽는다
       판별에 실패하면 억지로 읽지 않고 손을 든다

출력   그리드에 표시.  이상한 칸만 색칠
       [복사] 버튼으로 클립보드에 넣어 시스템에 붙여넣는다
```

조심할 것.

```text
실제 스캔본은 절대 커밋하지 않는다
  시료 지점명 · 담당자 이름 · 접수번호가 그대로 들어있다
  samples/ 는 .gitignore.  fixtures/ 에는 익명화한 것만

판별이 애매하면 손을 든다
  기기 소프트웨어가 업데이트되면 형식이 슬쩍 바뀐다
  제일 비슷한 규칙으로 억지로 읽으면 조용히 틀린 값이 나온다

사람에게 묻지 말고 표시만 한다
  멈춰 서서 물어보면 하나 답할 때마다 붙잡힌다
  최선값을 넣고 색칠해둔 뒤 넘어간다.  확인은 마지막에 한 번

판정하지 않는다
  "틀렸습니다"가 아니라 "확인해보세요" + 원본 조각
  누가 맞는지는 도구가 알 수 없다
```

## 문서를 읽는 순서

```text
docs/STATUS.md         ← 항상 이것부터. 현재 상태와 다음 작업
docs/DOMAIN.md         업무가 어떻게 돌아가는가. 사용자에게 들은 실무 배경
docs/NOTES-FORMATS.md  문서 형식. 실물을 보고 확인한 사실
docs/BACKLOG.md        미룬 것 · 버린 것과 그 이유
docs/SETUP.md          새 컴퓨터에서 처음 열 때만
```

전부 읽지 말 것. 기본은 STATUS.md 하나다.
다만 **판독이나 검증 로직을 건드릴 때는 DOMAIN.md 를 먼저 읽는다.**
그 코드가 왜 그렇게 생겼는지가 거기 있다.

무언가를 미루기로 했다면 STATUS가 아니라 BACKLOG에 **이유와 함께** 적는다.

## 실무 배경은 들을 때마다 적는다

사용자는 이 업무를 직접 하는 사람이다. 업무가 어떻게 돌아가는지,
왜 그렇게 하는지를 알려주면 **그 자리에서 `docs/DOMAIN.md` 에 적는다.**

```text
코드를 보면 아는 것        적지 않는다
git log 에 남는 것        적지 않는다
직접 듣지 않으면 모르는 것  ← 이것만 적는다
```

이런 것들이다.

```text
왜 이 문서가 이 시점에 나오는가
누가 무엇을 손으로 하고 있는가
어떤 제약 때문에 이렇게 하는가
무엇이 실제로는 안 쓰이는가
```

**미루면 사라진다.** 대화가 끝나면 다시 물어볼 수 없고,
다음 세션의 나는 이 대화를 기억하지 못한다. DOMAIN.md 가 유일한 통로다.

새 사실을 들으면 기존 내용과 어긋나는지 확인하고, 어긋나면 고친다.
추측은 적지 않는다. **확인된 것과 아직 모르는 것을 구분해서 적는다.**

## 스택

```text
Next.js (App Router) · React · TypeScript · Tailwind
Anthropic API        판독
pdf.js               화면에 PDF 그리기 · 원본 조각 잘라내기
Cloudflare Workers   배포
```

Next.js는 학습 데이터와 다를 수 있다. `node_modules/next/dist/docs/` 를 볼 것.

## 명령

```powershell
npm run dev        # localhost:3000
npx tsc --noEmit   # 타입 검사
npm run lint
```

명령 실행 전 `pwd`로 위치를 확인한다.

## 작업 방식

사용자는 웹 개발 경험이 있으나 **React / Next.js / AI API는 아직 익숙하지 않다.**
새 개념이 나오면 코드를 던지기 전에 무엇인지 · 왜 필요한지 설명한다.
단, 모든 내부 구현을 파고들지는 않는다.

**의미 있는 덩어리 단위로 작업한다.** 단계마다 문서 갱신과 커밋을 반복하면
의식이 작업보다 커진다. 눈에 보이는 결과가 나오는 단위로 묶는다.

화면 확인은 사용자가 직접 한다. AI는 `tsc` / `lint` / `build` 까지 책임진다.

노트북과 데스크탑 두 곳에서 작업한다. 세션은 이어지지 않으므로
**작업이 끝나면 STATUS.md를 갱신하고 push**한다. 이게 유일한 인수인계다.

## 문서는 초보자가 읽는다고 보고 쓴다

읽는 사람은 그 분야를 **처음 본다.** 네 가지를 담는다.

```text
배경   왜 이런 게 생겼나. 없던 시절엔 어떻게 했고 뭐가 불편했나
원리   실제로 어떻게 동작하나. 비유를 하나 붙인다
용어   전문 용어를 쉬운 말로 푼다
실무   그래서 여기서는 어떻게 쓰나. 뭘 조심하나
```

배경이 빠지면 외울 것이 되고, 원리가 빠지면 따라 하는 것이 되고,
실무가 빠지면 남의 이야기가 된다. 넷 다 있어야 자기 것이 된다.

**전문 용어는 처음 나올 때 반드시 푼다.** OCR · 파서 · 정규화 · 헛경보 같은
말을 설명 없이 쓰지 않는다. "다들 아는 말"이라고 넘기는 순간 그 문단은
읽는 사람에게 사라진다.

**비유는 하나만 쓰고, 안 맞는 지점을 함께 적는다.** 비유는 편한 만큼 반드시
틀린 데가 있다. 어디까지 유효한지를 적지 않으면 그 틀린 부분이 그대로 남는다.

설명이 끝나면 **"이렇게 설명하면 된다"** 로 실무자 말투 요약을 서너 줄 붙인다.
1겹은 처음 보는 사람 기준의 쉬운 말이고, 2겹은 그것을 줄인 게 아니라
더 조밀하고 "왜 그런가"를 한 겹 더 들어간 것이다. 그대로 소리 내어
말할 수 있어야 한다.

표 대신 코드블록으로 정렬한다. 굵게는 문단당 한두 번만 쓴다.

## 같은 내용을 여러 곳에 쓰지 않는다

```text
"이 줄이 왜 이런가"   → 코드 주석
"지금 어디까지 왔나"  → docs/STATUS.md
"개념이 무엇인가"     → docs/notes/
"무슨 일이 있었나"    → git log     (문서에 커밋 해시를 적지 않는다)
```

## 보안

이것은 **개인 프로젝트다.** 회사나 기관을 특정할 수 있는 것은 저장소에
들어가지 않는다. 코드에도 문서에도 커밋 기록에도 마찬가지다.

```text
넣지 않는다
  기관 이름 · 시스템 이름 · 부서명
  실제 시료 지점명 · 담당자 이름 · 접수번호
  실제 스캔본과 그 안의 모든 값
  회사 메일 주소 (커밋 작성자 포함)

대신 이렇게 쓴다
  "정보시스템" · "시험 기기" · "14번 시료" 같은 일반 명칭
  예시가 필요하면 지어낸 값을 쓴다
```

문서를 쓸 때마다 실제 이름이 섞여 들어가기 쉽다. 커밋 전에 한 번 훑는다.

```bash
grep -rniE "기관명|지점명|사람이름|회사도메인" --include="*.md" --include="*.ts" .
```

나머지 원칙.

```text
API 키는 서버에만 둔다.  클라이언트 코드에 넣지 않는다
기기 PC의 네트워크 · USB 격리를 우회하려는 시도는 하지 않는다
  인쇄물을 읽는 것은 우회가 아니다.  이미 반출이 허용된 결과물이다
```

## 커밋

```text
YYMMDD_작업내용     예) 260827_PROJECT_INIT
```
